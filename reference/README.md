# Reference Code

This directory is reserved for research and validation code that is not part of
the Thermox production dependency graph.

The MIT-licensed CO2 and water/steam IF97 projects have passed the initial
promotion gate. They now live under `modules/properties/`, provide isolated
library APIs, and are consumed through the Thermox physics property interface.
See [Property Packages](../docs/property-packages.md).
